# C File Requirements Analyzer

Система для автоматического анализа C/C++ кода и генерации тестируемых требований с использованием искусственного интеллекта.

## 🚀 Возможности

- **Автоматический анализ** C/C++ файлов (.c, .cpp, .h, .hpp)
- **Извлечение функций** с полной структурной информацией
- **Генерация требований** с использованием YandexGPT-5-Lite
- **Разбивка больших функций** на логические сегменты
- **Экспорт результатов** в Excel-таблицы
- **Веб-интерфейс** для удобной работы
- **Пакетная обработка** нескольких файлов

## 📋 Требования

- Python 3.8+
- 8GB+ оперативной памяти
- GPU (рекомендуется) для ускорения работы AI-модели

## 🛠 Установка

### 1. Клонирование репозитория

git clone <repository-url>
cd c-requirements-analyzer
2. Создание виртуального окружения

python -m venv venv
source venv/bin/activate  # Linux/MacOS
# или
venv\Scripts\activate     # Windows
3. Установка зависимостей

pip install -r requirements.txt
4. Создание необходимых директорий

python create_dirs.py
5. Загрузка AI-модели
Убедитесь, что модель YandexGPT-5-Lite-8B-instruct доступна в директории models/

🎯 Использование
Через веб-интерфейс (рекомендуется)
Запустите сервер:


python main.py
Откройте браузер и перейдите по адресу:

text
http://127.0.0.1:8080
Загрузите C/C++ файл через веб-форму

Скачайте готовый Excel-файл с требованиями

Через командную строку
Анализ одного файла
python
from c_to_excel_analyzer import analyze_c_file_to_excel

result = analyze_c_file_to_excel(
    c_file_path="example.c",
    system_prompt="Ваш кастомный промпт",
    max_tokens=2024
)
print(f"Результат сохранен: {result}")
Пакетный анализ
python
from c_to_excel_analyzer import batch_analyze_c_files

results = batch_analyze_c_files(
    c_files_dir="src/",
    output_dir="results/",
    delay_between_requests=1.0
)
📁 Структура проекта
text
c-requirements-analyzer/
├── main.py                 # FastAPI веб-сервер
├── ai_analysis.py          # Модуль работы с AI
├── c_parser.py             # Парсер C/C++ кода
├── c_to_excel_analyzer.py  # Основной анализатор
├── create_dirs.py          # Скрипт создания директорий
├── requirements.txt        # Зависимости проекта
├── uploads/               # Временные файлы загрузок
├── results/               # Результаты анализа
├── static/                # Статические файлы
└── templates/             # HTML шаблоны
📊 Формат выходных данных
Excel-файл содержит следующие колонки:

ИД: Уникальный идентификатор требования (LLR_01, LLR_02, ...)

Заголовок/Текст: Текст требования или элемента функции

Требование: Флаг ("да"/"нет") является ли строка требованием

Функция: Имя анализируемой функции

Имя файла: Исходный файл

⚙️ Настройка
Кастомный системный промпт
Вы можете изменить промпт для AI в файле main.py:

python
CUSTOM_SYSTEM_PROMPT = (
    "Ты — инженер по требованиям. Сформулируй чёткие, тестируемые требования..."
)
Параметры генерации
max_tokens: Максимальное количество токенов для генерации

temperature: Температура генерации (0.0 для детерминированных результатов)

delay_between_requests: Задержка между запросами к AI

🔧 Разработка
Добавление новой функциональности
Модифицируйте c_parser.py для расширения анализа кода

Обновите ai_analysis.py для изменения логики генерации требований

Добавьте новые колонки в c_to_excel_analyzer.py

Тестирование

# Создайте тестовый C-файл
echo "int test_function(int a, int b) { return a + b; }" > test.c

# Запустите анализ
python -c "from c_to_excel_analyzer import analyze_c_file_to_excel; analyze_c_file_to_excel('test.c')"
🐛 Устранение неисправностей
Проблемы с кодировкой
Если возникают ошибки чтения файлов, проверьте кодировку исходных файлов. Система поддерживает UTF-8 и CP1251.

Нехватка памяти
Для больших моделей используйте:

python
# В ai_analysis.py
model = AutoModelForCausalLM.from_pretrained(
    MODEL_ID,
    torch_dtype=torch.float16,  # Использование половинной точности
    device_map="auto"
)
Медленная работа
Убедитесь, что используется GPU

Увеличьте delay_between_requests для снижения нагрузки

Используйте пакетную обработку для нескольких файлов

📄 Лицензия
[Укажите вашу лицензию]

🤝 Вклад в проект
Форкните репозиторий

Создайте ветку для функциональности (git checkout -b feature/AmazingFeature)

Закоммитьте изменения (git commit -m 'Add some AmazingFeature')

Запушьте в ветку (git push origin feature/AmazingFeature)

Откройте Pull Request

📞 Поддержка
По вопросам использования и проблемам создавайте issues в репозитории проекта.